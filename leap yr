#include  <bits/stdc++.h>
#include <iostream>
using namespace std;

bool leap(int n){
    if ((n%400 == 0) || (n%4 == 0 && n%100 != 0)){
        return true;
    }
    return false;
}

int main(){
    int n;
    cout<<"enter the number: ";
    cin>>n;
    
    cout<< leap(n);
    
}
