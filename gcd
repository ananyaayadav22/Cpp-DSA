#include  <bits/stdc++.h>
#include <iostream>
using namespace std;

int gcd(int a, int b){
    if (b==0){
        return a;
    }
    
    return gcd(b, a%b);
}

int main(){
    cout<<"enter the numbers: ";
    
    int m,n;
    cin>>m>>n;
    
    cout<<"GCD: "<<gcd(m,n);
}
